import type { NextConfig } from "next";

// basePath / assetPrefix are injected by `webflow cloud deploy` from the
// environment's mount path (/demos) — do not hard-code them here.
const nextConfig: NextConfig = {};

export default nextConfig;
